# Calculator Pytest Fixtures

Simple calculator application implemented in Python and tested with pytest fixtures.

## Installation

Clone the repository and install dependencies:

```bash
git clone https://github.com/tolik-0/calculator_pytest_fixtures.git
cd calculator_pytest_fixtures
pip install pytest
```

## Running the application

```bash
python calculator.py
```

## Running tests

```bash
pytest -v
```
